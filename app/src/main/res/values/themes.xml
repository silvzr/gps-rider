<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GPSRider" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#34495E</item>
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Make status bar icons dark/light based on theme -->
        <item name="android:windowLightStatusBar">true</item>
        <!-- Remove window animations -->
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Make app start immediately -->
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources> 